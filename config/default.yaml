mode: copilot
hotkey: "<ctrl>+<alt>+w"

llm:
  base_url: ""         # 空则读环境变量 OPENAI_BASE_URL
  api_key: ""          # 空则读 OPENAI_API_KEY；安装后请在客户端设置里填写
  model: gpt-4o-mini

layout:
  incoming_max_x: 0.48
  outgoing_min_x: 0.52

policy:
  auto_scenes:
    - smalltalk
    - logistics
  max_auto_per_hour: 20
